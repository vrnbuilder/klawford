{%- comment -%} Dedicated product page — Klawford Duo Sleep {%- endcomment -%}

{%- assign kl_first_variant = product.selected_or_first_available_variant -%}
{%- assign kl_on_sale = false -%}
{%- if product.compare_at_price > product.price -%}{%- assign kl_on_sale = true -%}{%- endif -%}
{%- assign kl_sale_pct = 0 -%}
{%- if kl_on_sale -%}
  {%- assign kl_sale_pct = product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price | round -%}
{%- endif -%}

<div class="klm-page">

  {%- comment -%} ── BREADCRUMB ──────────────────────────────────────────── {%- endcomment -%}
  <div class="container">
    <nav class="klm-breadcrumb" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <svg width="8" height="8" viewBox="0 0 10 10" fill="none"><path d="M3 1l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg>
      {%- if collection -%}
        <a href="{{ collection.url }}">{{ collection.title }}</a>
        <svg width="8" height="8" viewBox="0 0 10 10" fill="none"><path d="M3 1l4 4-4 4" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg>
      {%- endif -%}
      <span>{{ product.title }}</span>
    </nav>
  </div>

  {%- comment -%} ── PRODUCT TOP: GALLERY + INFO ──────────────────────────── {%- endcomment -%}
  <div class="klm-top container">

    {%- comment -%} ── GALLERY ─────────────────────────────────────────────── {%- endcomment -%}
    <div class="klm-gallery">
      <div class="klm-gallery__main" id="klm-gallery-main">
        {%- if product.featured_image -%}
          <img
            id="klm-main-img"
            class="klm-gallery__main-img"
            src="{{ product.featured_image | image_url: width: 900 }}"
            srcset="{{ product.featured_image | image_url: width: 600 }} 600w,
                    {{ product.featured_image | image_url: width: 900 }} 900w,
                    {{ product.featured_image | image_url: width: 1200 }} 1200w"
            sizes="(max-width: 768px) 100vw, 50vw"
            alt="{{ product.featured_image.alt | escape | default: product.title }}"
            width="900" height="675"
            loading="eager" fetchpriority="high">
        {%- else -%}
          <div class="klm-gallery__placeholder">
            <svg width="80" height="60" viewBox="0 0 80 60" fill="none" aria-hidden="true">
              <rect x="4" y="8" width="72" height="44" rx="4" stroke="currentColor" stroke-width="2"/>
              <rect x="10" y="16" width="60" height="28" rx="2" stroke="currentColor" stroke-width="1.5" stroke-dasharray="4 3"/>
              <circle cx="28" cy="26" r="5" stroke="currentColor" stroke-width="1.5"/>
              <path d="M16 40l18-14 14 10 10-8 10 12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            <p>Upload product images in Shopify Admin</p>
          </div>
        {%- endif -%}
        <span class="klm-gallery__badge">
          <svg width="12" height="12" viewBox="0 0 14 14" fill="none"><circle cx="7" cy="7" r="6" stroke="currentColor" stroke-width="1.3"/><path d="M7 4v3l2 2" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
          60 Night Risk-Free Trial
        </span>
        <button class="klm-gallery__expand" id="klm-expand" type="button" aria-label="Expand image">
          <svg width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M1 6V1h5M10 1h5v5M15 10v5h-5M6 15H1v-5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Click To Expand
        </button>
      </div>

      {%- if product.images.size > 1 -%}
        <div class="klm-gallery__thumb-wrap">
          <button class="klm-gallery__arrow" id="klm-thumb-prev" type="button" aria-label="Previous images">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M9 2L4 7l5 5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
          <div class="klm-gallery__thumbs" id="klm-thumbs">
            {%- for image in product.images -%}
              <button
                class="klm-gallery__thumb{% if forloop.first %} is-active{% endif %}"
                type="button"
                data-src="{{ image | image_url: width: 900 }}"
                data-width="{{ image.width }}"
                data-height="{{ image.height }}"
                aria-label="View image {{ forloop.index }}">
                <img src="{{ image | image_url: width: 160 }}" alt="{{ image.alt | escape | default: product.title }}" width="80" height="80" loading="lazy">
              </button>
            {%- endfor -%}
          </div>
          <button class="klm-gallery__arrow" id="klm-thumb-next" type="button" aria-label="Next images">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M5 2l5 5-5 5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </button>
        </div>
      {%- else -%}
        <div class="klm-gallery__thumb-wrap">
          {%- for i in (1..5) -%}
            <div class="klm-gallery__thumb-ph">
              <svg width="24" height="18" viewBox="0 0 80 60" fill="none" aria-hidden="true"><rect x="4" y="8" width="72" height="44" rx="4" stroke="currentColor" stroke-width="3"/><circle cx="28" cy="26" r="7" stroke="currentColor" stroke-width="2.5"/><path d="M16 46l18-18 14 12 10-10 10 16" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"/></svg>
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    {%- comment -%} ── PRODUCT INFO ──────────────────────────────────────── {%- endcomment -%}
    <div class="klm-info">
      <h1 class="klm-info__title">{{ product.title }}</h1>

      <div class="klm-info__price-row" id="klm-price-row">
        <span class="klm-info__price" id="klm-price">{{ kl_first_variant.price | money }}</span>
        {%- if kl_on_sale -%}
          <span class="klm-info__compare" id="klm-compare">{{ kl_first_variant.compare_at_price | money }}</span>
          <span class="klm-info__pct" id="klm-pct">{{ kl_sale_pct }}% OFF</span>
        {%- else -%}
          <span class="klm-info__compare" id="klm-compare" style="display:none;"></span>
          <span class="klm-info__pct" id="klm-pct" style="display:none;"></span>
        {%- endif -%}
      </div>

      {%- form 'product', product, id: 'klm-form' -%}
        {%- unless product.has_only_default_variant -%}
          {%- for option in product.options_with_values -%}
            {%- assign _olc = option.name | downcase -%}
            {%- if _olc == 'size' -%}
              {%- assign _selected_price = kl_first_variant.price -%}
              <div class="klm-size-wrap">
                <div class="klm-size-box">
                  <div class="klm-size-header">
                    <span class="klm-size-header__label">Select Size</span>
                    <span class="klm-size-header__current" id="klm-size-name">{{ option.selected_value }}</span>
                  </div>
                  <div class="klm-size-grid">
                    {%- for value in option.values -%}
                      {%- assign _sv_price = 0 -%}
                      {%- assign _sv_compare = 0 -%}
                      {%- assign _sv_id = '' -%}
                      {%- assign _sv_avail = false -%}
                      {%- for v in product.variants -%}
                        {%- if v.option1 == value or v.option2 == value or v.option3 == value -%}
                          {%- assign _sv_price = v.price -%}
                          {%- assign _sv_compare = v.compare_at_price -%}
                          {%- assign _sv_id = v.id -%}
                          {%- assign _sv_avail = v.available -%}
                          {%- break -%}
                        {%- endif -%}
                      {%- endfor -%}
                      {%- assign _price_diff = _sv_price | minus: _selected_price -%}
                      {%- assign _vlc = value | downcase -%}
                      {%- case _vlc -%}
                        {%- when 'single' -%}{%- assign _dim_w = 90 -%}{%- assign _dim_l = 190 -%}
                        {%- when 'small double' -%}{%- assign _dim_w = 120 -%}{%- assign _dim_l = 190 -%}
                        {%- when 'double' -%}{%- assign _dim_w = 135 -%}{%- assign _dim_l = 190 -%}
                        {%- when 'king' -%}{%- assign _dim_w = 150 -%}{%- assign _dim_l = 200 -%}
                        {%- when 'super king' -%}{%- assign _dim_w = 180 -%}{%- assign _dim_l = 200 -%}
                        {%- else -%}{%- assign _dim_w = '' -%}{%- assign _dim_l = '' -%}
                      {%- endcase -%}
                      <button type="button"
                        class="klm-size-btn{% if option.selected_value == value %} is-active{% endif %}{% unless _sv_avail %} is-soldout{% endunless %}"
                        data-size="{{ value | escape }}"
                        data-vid="{{ _sv_id }}"
                        data-price="{{ _sv_price }}"
                        data-compare="{{ _sv_compare }}"
                        data-width="{{ _dim_w }}"
                        data-length="{{ _dim_l }}"
                        aria-pressed="{% if option.selected_value == value %}true{% else %}false{% endif %}">
                        <div class="klm-size-btn__icon" aria-hidden="true">
                          <svg viewBox="0 0 60 44" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <rect x="2" y="2" width="56" height="40" rx="4" stroke="currentColor" stroke-width="1.6"/>
                            <rect x="2" y="2" width="56" height="11" rx="4" stroke="currentColor" stroke-width="1" fill="currentColor" fill-opacity=".06"/>
                            <circle cx="15" cy="24" r="2.5" fill="currentColor" opacity=".55"/>
                            <circle cx="30" cy="24" r="2.5" fill="currentColor" opacity=".55"/>
                            <circle cx="45" cy="24" r="2.5" fill="currentColor" opacity=".55"/>
                            <circle cx="15" cy="35" r="2.5" fill="currentColor" opacity=".55"/>
                            <circle cx="30" cy="35" r="2.5" fill="currentColor" opacity=".55"/>
                            <circle cx="45" cy="35" r="2.5" fill="currentColor" opacity=".55"/>
                          </svg>
                        </div>
                        <span class="klm-size-btn__name">{{ value }}</span>
                        {%- if option.selected_value == value -%}
                          <span class="klm-size-btn__price" data-role="price">{{ _sv_price | money }}</span>
                        {%- elsif _price_diff < 0 -%}
                          {%- assign _diff_abs = _price_diff | abs -%}
                          <span class="klm-size-btn__price klm-size-btn__price--save" data-role="price">-{{ _diff_abs | money }}</span>
                        {%- elsif _price_diff > 0 -%}
                          <span class="klm-size-btn__price" data-role="price">+{{ _price_diff | money }}</span>
                        {%- else -%}
                          <span class="klm-size-btn__price" data-role="price">{{ _sv_price | money }}</span>
                        {%- endif -%}
                      </button>
                    {%- endfor -%}
                  </div>
                  {%- assign _sel_vlc = option.selected_value | downcase -%}
                  {%- case _sel_vlc -%}
                    {%- when 'single' -%}{%- assign _init_w = 90 -%}{%- assign _init_l = 190 -%}
                    {%- when 'small double' -%}{%- assign _init_w = 120 -%}{%- assign _init_l = 190 -%}
                    {%- when 'double' -%}{%- assign _init_w = 135 -%}{%- assign _init_l = 190 -%}
                    {%- when 'king' -%}{%- assign _init_w = 150 -%}{%- assign _init_l = 200 -%}
                    {%- when 'super king' -%}{%- assign _init_w = 180 -%}{%- assign _init_l = 200 -%}
                    {%- else -%}{%- assign _init_w = '' -%}{%- assign _init_l = '' -%}
                  {%- endcase -%}
                  {%- if _init_w != '' -%}
                  <div class="klm-size-dims" id="klm-size-dims">
                    <span>Width: {{ _init_w }}cm</span>
                    <span>Length: {{ _init_l }}cm</span>
                  </div>
                  {%- endif -%}
                </div>
              </div>
            {%- endif -%}
          {%- endfor -%}
        {%- endunless -%}

        <input type="hidden" name="id" id="klm-variant-id" value="{{ kl_first_variant.id }}">

        <button
          type="submit"
          class="btn btn--primary btn--full klm-atc"
          id="klm-atc-btn"
          data-add-to-cart
          {%- unless kl_first_variant.available -%}disabled{%- endunless -%}>
          <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true"><path d="M1 1h2.5l1.7 8.6a2 2 0 0 0 2 1.4h7.3a2 2 0 0 0 2-1.6L17.5 4H4.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/><circle cx="7" cy="15.5" r="1.5" stroke="currentColor" stroke-width="1.4"/><circle cx="13.5" cy="15.5" r="1.5" stroke="currentColor" stroke-width="1.4"/></svg>
          <span id="klm-atc-label">{%- if kl_first_variant.available -%}Add to Basket{%- else -%}Sold Out{%- endif -%}</span>
        </button>
      {%- endform -%}

      <div class="klm-payment-info">
        <div class="klm-payment-info__row">
          <svg width="16" height="12" viewBox="0 0 18 14" fill="none" aria-hidden="true"><path d="M1 2h11v9H1V2z" stroke="currentColor" stroke-width="1.3"/><path d="M12 5h3l2 3v3h-5V5z" stroke="currentColor" stroke-width="1.3"/><circle cx="4" cy="12" r="1.5" stroke="currentColor" stroke-width="1.3"/><circle cx="14" cy="12" r="1.5" stroke="currentColor" stroke-width="1.3"/></svg>
          <span><strong>Fast Free delivery.</strong> Select date at checkout.</span>
        </div>
        <div class="klm-payment-info__row">
          <span class="klm-logo klm-logo--dividebuy">Divide<em>Buy</em></span>
          <span>{{ section.settings.dividebuy_text | default: 'Spread the cost interest-free over 12 months, 0% credit.' }}</span>
        </div>
        <div class="klm-payment-info__row" style="border-bottom:none;">
          <span class="klm-logo klm-logo--klarna">Klarna.</span>
          <span>{{ section.settings.klarna_text | default: 'Pay later in 30 days or split into 3 interest-free payments.' }}</span>
        </div>
        <div class="klm-payment-info__icons">
          {%- render 'payment-icons', extended: true -%}
        </div>
      </div>

    </div>
  </div>
</div>

{%- comment -%} ── TRUST STRIP ─────────────────────────────────────────────── {%- endcomment -%}
<div class="klm-trust-strip">
  <div class="container">
    <div class="klm-trust-strip__inner">
      <div class="klm-trust-item">
        <svg width="24" height="24" viewBox="0 0 26 26" fill="none" aria-hidden="true"><path d="M13 3l2.5 5 5.5.8-4 3.9 1 5.5-5-2.6L8 17.2l1-5.5L5 7.8l5.5-.8L13 3z" stroke="currentColor" stroke-width="1.4" stroke-linejoin="round"/></svg>
        <div><strong>5 Year Warranty</strong><span>On every mattress</span></div>
      </div>
      <div class="klm-trust-item">
        <svg width="24" height="24" viewBox="0 0 26 26" fill="none" aria-hidden="true"><circle cx="13" cy="13" r="9" stroke="currentColor" stroke-width="1.4"/><path d="M13 8v5l3 3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg>
        <div><strong>60 Night Risk-Free Trial</strong><span>Sleep on it first</span></div>
      </div>
      <div class="klm-trust-item">
        <svg width="24" height="24" viewBox="0 0 26 26" fill="none" aria-hidden="true"><rect x="3" y="6" width="20" height="4" rx="1" stroke="currentColor" stroke-width="1.4"/><path d="M5 10h16v10a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V10z" stroke="currentColor" stroke-width="1.4"/><path d="M10 10v12M16 10v12" stroke="currentColor" stroke-width="1.2" stroke-dasharray="2 2"/></svg>
        <div><strong>0% Interest Rate</strong><span>Spread the cost</span></div>
      </div>
      <div class="klm-trust-item">
        <svg width="24" height="24" viewBox="0 0 26 26" fill="none" aria-hidden="true"><path d="M3 18h20M3 18l3-8h14l3 8M9 18v-3m8 3v-3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/><circle cx="7" cy="20" r="1.5" stroke="currentColor" stroke-width="1.3"/><circle cx="19" cy="20" r="1.5" stroke="currentColor" stroke-width="1.3"/></svg>
        <div><strong>Fast Free Delivery</strong><span>Room of your choice</span></div>
      </div>
      <div class="klm-trust-item">
        <svg width="24" height="24" viewBox="0 0 26 26" fill="none" aria-hidden="true"><rect x="4" y="6" width="18" height="3" rx="1" stroke="currentColor" stroke-width="1.4"/><rect x="3" y="9" width="20" height="12" rx="2" stroke="currentColor" stroke-width="1.4"/><path d="M9 15h8M9 18h5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
        <div><strong>UK Quality Materials</strong><span>Built to last</span></div>
      </div>
    </div>
  </div>
</div>
{%- comment -%} ── WHAT MAKES OUR MATTRESS GREAT ───────────────────────────── {%- endcomment -%}
<section class="klm-features" id="klm-features">
  <div class="container">
    <h2 class="klm-section__heading">What Makes Our Mattress Great</h2>
    <div class="klm-features__body">

      <div class="klm-features__image">
        {%- if product.images[1] -%}
          <img
            src="{{ product.images[1] | image_url: width: 700 }}"
            alt="{{ product.title }} — construction layers"
            width="700" height="560"
            loading="lazy"
            class="klm-features__image-img">
        {%- elsif product.featured_image -%}
          <img
            src="{{ product.featured_image | image_url: width: 700 }}"
            alt="{{ product.title }}"
            width="700" height="560"
            loading="lazy"
            class="klm-features__image-img">
        {%- endif -%}
      </div>

      <div class="klm-features__right">
        <div class="klm-features__icon-row" id="klm-feat-icons" role="tablist">
          {%- assign _feat_names = 'Orthopaedic Core,Bamboo Cover,Triple Foam,Pressure Relief,Temperature,Couple-Friendly,Medium-Firm,UK Quality' | split: ',' -%}
          {%- for f in _feat_names -%}
            <button class="klm-feat{% if forloop.first %} is-active{% endif %}" role="tab" data-feat="{{ forloop.index0 }}" type="button" aria-selected="{% if forloop.first %}true{% else %}false{% endif %}">
              <div class="klm-feat__icon" aria-hidden="true">
                {%- case forloop.index0 -%}
                  {%- when 0 -%}<!-- Orthopaedic Core: spine -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <circle cx="22" cy="7" r="4" fill="#2E86C1"/>
                      <rect x="19" y="13" width="6" height="5" rx="1.5" fill="#2E86C1"/>
                      <rect x="19" y="20" width="6" height="5" rx="1.5" fill="#2980B9" opacity=".8"/>
                      <rect x="19" y="27" width="6" height="5" rx="1.5" fill="#2980B9" opacity=".6"/>
                      <path d="M13 42L19 34H25L31 42" stroke="#2E86C1" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
                      <line x1="19" y1="34" x2="25" y2="34" stroke="#2E86C1" stroke-width="2"/>
                    </svg>
                  {%- when 1 -%}<!-- Bamboo Cover: leaf -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <circle cx="22" cy="22" r="18" fill="#EAFAF1"/>
                      <path d="M22 34C22 34 10 26 12 16C15 8 22 10 22 10C22 10 29 8 32 16C34 26 22 34 22 34Z" fill="#27AE60"/>
                      <line x1="22" y1="10" x2="22" y2="34" stroke="#EAFAF1" stroke-width="1.5" stroke-linecap="round"/>
                      <path d="M22 20C18 18 14 19 12 22" stroke="#EAFAF1" stroke-width="1" stroke-linecap="round"/>
                      <path d="M22 26C26 24 30 25 32 28" stroke="#EAFAF1" stroke-width="1" stroke-linecap="round"/>
                    </svg>
                  {%- when 2 -%}<!-- Triple Foam: stacked layers -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <rect x="6" y="10" width="32" height="7" rx="2" fill="#AED6F1" stroke="#2E86C1" stroke-width="1.3"/>
                      <rect x="6" y="19" width="32" height="7" rx="2" fill="#85C1E9" stroke="#2E86C1" stroke-width="1.3"/>
                      <rect x="6" y="28" width="32" height="7" rx="2" fill="#5DADE2" stroke="#2E86C1" stroke-width="1.3"/>
                      <text x="22" y="16" text-anchor="middle" fill="#1A5276" font-size="5" font-weight="bold">1</text>
                      <text x="22" y="25" text-anchor="middle" fill="#1A5276" font-size="5" font-weight="bold">2</text>
                      <text x="22" y="34" text-anchor="middle" fill="#FFFFFF" font-size="5" font-weight="bold">3</text>
                    </svg>
                  {%- when 3 -%}<!-- Pressure Relief: body on mattress -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <rect x="4" y="31" width="36" height="9" rx="2" fill="#EBF5FB" stroke="#2E86C1" stroke-width="1.5"/>
                      <circle cx="11" cy="22" r="5" fill="#E67E22"/>
                      <path d="M16 22H32C34 22 36 24 36 26V31H8V26C8 24 10 22 16 22Z" fill="#F0A04B" opacity=".85"/>
                      <ellipse cx="11" cy="24" rx="3" ry="2" fill="#CA6F1E" opacity=".7"/>
                      <ellipse cx="26" cy="24" rx="4" ry="2" fill="#CA6F1E" opacity=".5"/>
                    </svg>
                  {%- when 4 -%}<!-- Temperature: thermometer -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <rect x="16" y="5" width="12" height="26" rx="6" fill="#EBF5FB" stroke="#bbb" stroke-width="1.5"/>
                      <rect x="19" y="19" width="6" height="12" rx="3" fill="#3498DB"/>
                      <circle cx="22" cy="36" r="7" fill="#27AE60"/>
                      <circle cx="22" cy="36" r="4" fill="#1E8449"/>
                      <path d="M28 9H31M28 14H31M28 19H31" stroke="#bbb" stroke-width="1.5" stroke-linecap="round"/>
                    </svg>
                  {%- when 5 -%}<!-- Couple-Friendly: two zones -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <rect x="3" y="29" width="38" height="11" rx="3" fill="#EBF5FB" stroke="#2E86C1" stroke-width="1.5"/>
                      <line x1="22" y1="6" x2="22" y2="40" stroke="#5D6D7E" stroke-width="1.5" stroke-dasharray="4 3"/>
                      <circle cx="11" cy="18" r="5" fill="#27AE60"/>
                      <circle cx="33" cy="18" r="5" fill="#27AE60"/>
                      <path d="M9 29Q11 24 11 29" stroke="#27AE60" stroke-width="2" fill="none" stroke-linecap="round"/>
                      <path d="M31 29Q33 24 33 29" stroke="#27AE60" stroke-width="2" fill="none" stroke-linecap="round"/>
                    </svg>
                  {%- when 6 -%}<!-- Medium-Firm: balance scale -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <line x1="22" y1="8" x2="22" y2="38" stroke="#2E86C1" stroke-width="2" stroke-linecap="round"/>
                      <line x1="10" y1="38" x2="34" y2="38" stroke="#2E86C1" stroke-width="2" stroke-linecap="round"/>
                      <line x1="22" y1="14" x2="10" y2="22" stroke="#2E86C1" stroke-width="1.5"/>
                      <line x1="22" y1="14" x2="34" y2="22" stroke="#2E86C1" stroke-width="1.5"/>
                      <ellipse cx="10" cy="24" rx="7" ry="4" fill="#AED6F1" stroke="#2E86C1" stroke-width="1.3"/>
                      <ellipse cx="34" cy="24" rx="7" ry="4" fill="#AED6F1" stroke="#2E86C1" stroke-width="1.3"/>
                      <circle cx="22" cy="8" r="3" fill="#2E86C1"/>
                    </svg>
                  {%- when 7 -%}<!-- UK Quality: Union Jack -->
                    <svg width="44" height="44" viewBox="0 0 44 44" fill="none">
                      <defs><clipPath id="ds-flag"><rect x="4" y="10" width="36" height="24" rx="3"/></clipPath></defs>
                      <g clip-path="url(#ds-flag)">
                        <rect x="4" y="10" width="36" height="24" fill="#012169"/>
                        <line x1="4" y1="10" x2="40" y2="34" stroke="white" stroke-width="9"/>
                        <line x1="40" y1="10" x2="4" y2="34" stroke="white" stroke-width="9"/>
                        <line x1="4" y1="10" x2="40" y2="34" stroke="#C8102E" stroke-width="5"/>
                        <line x1="40" y1="10" x2="4" y2="34" stroke="#C8102E" stroke-width="5"/>
                        <rect x="4" y="18" width="36" height="8" fill="white"/>
                        <rect x="18" y="10" width="8" height="24" fill="white"/>
                        <rect x="4" y="19.5" width="36" height="5" fill="#C8102E"/>
                        <rect x="19.5" y="10" width="5" height="24" fill="#C8102E"/>
                      </g>
                      <rect x="4" y="10" width="36" height="24" rx="3" stroke="#ddd" stroke-width="1" fill="none"/>
                    </svg>
                {%- endcase -%}
              </div>
              <span class="klm-feat__label">{{ f }}</span>
            </button>
          {%- endfor -%}
        </div>

        <div class="klm-features__track" aria-hidden="true">
          <span class="klm-features__pip" id="klm-feat-pip"></span>
        </div>

        <div class="klm-features__detail">
          <h3 class="klm-features__detail-title" id="klm-feat-title">Orthopaedic Foam Core</h3>
          <p class="klm-features__detail-body" id="klm-feat-body">The Duo Sleep is built around a 25 cm orthopaedic foam core engineered for spine alignment and firm, even support across the entire sleeping surface — giving your back the reinforcement it needs, night after night.</p>
        </div>
      </div>

    </div>
  </div>
</section>

{%- comment -%} ── OVERVIEW ────────────────────────────────────────────────── {%- endcomment -%}
<section class="klm-section klm-overview">
  <div class="container">
    <h2 class="klm-section__heading">Overview</h2>
    <div class="klm-overview__body rte">
      {{ product.description }}
    </div>
  </div>
</section>

{%- comment -%} ── SLEEP TECHNOLOGY ─────────────────────────────────────────── {%- endcomment -%}
<section class="klm-sleep-tech">
  <div class="container">
    <div class="klm-sleep-tech__inner">
      <div class="klm-sleep-tech__icon" aria-hidden="true">
        <svg width="56" height="56" viewBox="0 0 56 56" fill="none"><path d="M28 8a20 20 0 0 0 20 20A20 20 0 0 1 28 48 20 20 0 0 1 8 28 20 20 0 0 0 28 8z" stroke="currentColor" stroke-width="2"/><circle cx="38" cy="16" r="3" stroke="currentColor" stroke-width="1.5"/><circle cx="44" cy="22" r="2" stroke="currentColor" stroke-width="1.5"/></svg>
      </div>
      <h2 class="klm-sleep-tech__heading">Comfort Designed Around You</h2>
      <p class="klm-sleep-tech__body">The Duo Sleep is engineered for people who want the feel of a luxury hotel mattress at home — medium-firm support, natural breathability, and a cover that works with your body, not against it.</p>
      <h3 class="klm-sleep-tech__sub">What the Duo Sleep Delivers</h3>
      <ul class="klm-sleep-tech__list">
        <li>
          <div class="klm-sleep-tech__bullet-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><rect x="2" y="7" width="16" height="6" rx="1" stroke="currentColor" stroke-width="1.3"/><path d="M4 7V5m3 2V4m3 3V5m3 2V4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/><path d="M4 13v2m3-2v1m3-1v2m3-2v1" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
          </div>
          <span>Three layers of medium-firm foam cushion pressure points at the hips, shoulders, and lower back — so you wake up feeling rested whatever position you sleep in.</span>
        </li>
        <li>
          <div class="klm-sleep-tech__bullet-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M10 2a8 8 0 0 0 8 8A8 8 0 0 1 10 18 8 8 0 0 1 2 10 8 8 0 0 0 10 2z" stroke="currentColor" stroke-width="1.3"/></svg>
          </div>
          <span>The bamboo knitted cover naturally regulates your sleeping temperature — drawing moisture and warmth away from the surface to keep you cool and comfortable all night.</span>
        </li>
        <li>
          <div class="klm-sleep-tech__bullet-icon" aria-hidden="true">
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none"><path d="M4 10h12M4 6l4 4-4 4M12 6l4 4-4 4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <span>The foam construction absorbs and minimises partner movement — so you sleep undisturbed even when your partner shifts or gets up during the night.</span>
        </li>
      </ul>
    </div>
  </div>
</section>

{%- comment -%} ── MATTRESS LAYERS ──────────────────────────────────────────── {%- endcomment -%}
{%- if product.images[2] -%}
<section class="klm-section klm-layers">
  <div class="container">
    <div class="klm-layers__img-wrap">
      <img
        src="{{ product.images[2] | image_url: width: 900 }}"
        alt="{{ product.title }} — mattress construction layers"
        class="klm-layers__img"
        loading="lazy"
        width="900">
    </div>
  </div>
</section>
{%- endif -%}

{%- comment -%} ── FINANCE SECTION ──────────────────────────────────────────── {%- endcomment -%}
<div class="klm-finance" {% if product.images[3] %}style="background-image:url('{{ product.images[3] | image_url: width: 1600 }}');"{% endif %}>
  <div class="klm-finance__card">
    <p class="klm-finance__intro">Shop Now, Pay Later with</p>
    <div class="klm-finance__logo">Divide<em>Buy</em></div>
    <div class="klm-finance__banner">0% INTEREST-FREE INSTALMENTS</div>
    <ul class="klm-finance__list">
      <li>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><rect x="2" y="4" width="16" height="12" rx="2" stroke="currentColor" stroke-width="1.3"/><path d="M5 10h10M5 13h6" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/></svg>
        <div><strong>Pay In Monthly Instalments</strong><span>Spread the cost of your order for up to 12 months.</span></div>
      </li>
      <li>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><circle cx="10" cy="10" r="7.5" stroke="currentColor" stroke-width="1.3"/><path d="M7 10l2 2 4-4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
        <div><strong>Zero Interest Rate</strong><span>Every purchase is entirely interest-free, no matter the price or term length.</span></div>
      </li>
      <li>
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><path d="M10 3v14M5 8l5-5 5 5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round" stroke-linejoin="round"/></svg>
        <div><strong>No Early Repayment Charges</strong><span>The recurring card payments are collected monthly until your balance is paid.</span></div>
      </li>
    </ul>
  </div>
</div>

{%- comment -%} ── SERVICE SECTION + SECOND ATC ─────────────────────────────── {%- endcomment -%}
<section class="klm-service">
  <div class="container">
    <h2 class="klm-section__heading">Sleep Specialists At Your Service</h2>
    <div class="klm-service__inner">
      <div class="klm-service__image">
        {%- if product.images.size > 4 -%}
          <img
            src="{{ product.images[4] | image_url: width: 600 }}"
            alt="{{ product.images[4].alt | escape | default: product.title }}"
            width="600" height="480"
            loading="lazy"
            class="klm-service__image-img">
        {%- elsif product.featured_image -%}
          <img
            src="{{ product.featured_image | image_url: width: 600 }}"
            alt="{{ product.title }}"
            width="600" height="480"
            loading="lazy"
            class="klm-service__image-img">
        {%- else -%}
          <div class="klm-service__image-ph">
            <svg width="64" height="48" viewBox="0 0 80 60" fill="none" aria-hidden="true"><rect x="4" y="8" width="72" height="44" rx="4" stroke="currentColor" stroke-width="2.5"/><circle cx="28" cy="28" r="8" stroke="currentColor" stroke-width="2"/><path d="M16 44l16-16 14 12 10-10 10 14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
            <span>Upload product images to display here</span>
          </div>
        {%- endif -%}
      </div>

      <div class="klm-service__points">
        <div class="klm-service__point">
          <div class="klm-service__point-icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M4 20h20M4 20l3-8h14l3 8M10 20v-3m8 3v-3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><circle cx="7" cy="22" r="1.5" stroke="currentColor" stroke-width="1.3"/><circle cx="21" cy="22" r="1.5" stroke="currentColor" stroke-width="1.3"/></svg>
          </div>
          <div>
            <strong>Delivered to Your Room</strong>
            <p>We deliver with a 2-man team straight to the room of your choice. Choose your preferred delivery date at checkout.</p>
          </div>
        </div>
        <div class="klm-service__point">
          <div class="klm-service__point-icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><rect x="3" y="10" width="22" height="14" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M8 10V8a6 6 0 0 1 12 0v2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><circle cx="14" cy="17" r="2" stroke="currentColor" stroke-width="1.5"/></svg>
          </div>
          <div>
            <strong>Hassle-Free Returns</strong>
            <p>Not right for you? Contact us within your 60-night trial window and we will arrange a collection and full refund.</p>
          </div>
        </div>
        <div class="klm-service__point">
          <div class="klm-service__point-icon" aria-hidden="true">
            <svg width="28" height="28" viewBox="0 0 28 28" fill="none"><path d="M25 19.4v3a2 2 0 0 1-2.2 2 23 23 0 0 1-9.8-3.5 22.6 22.6 0 0 1-6.8-6.8 23 23 0 0 1-3.5-9.9A2 2 0 0 1 4.6 2h3a2 2 0 0 1 2 1.7c.13 1.07.37 2.13.72 3.16a2 2 0 0 1-.45 2.1l-1.27 1.28a16 16 0 0 0 5.84 5.84l1.28-1.28a2 2 0 0 1 2.1-.45c1.03.35 2.09.59 3.16.72A2 2 0 0 1 25 19.4z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          </div>
          <div>
            <strong>We're Just a Phone Call Away</strong>
            <p>Got questions or need recommendations? Call us at 01924 926117 and we'll be happy to help you find the right mattress.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="klm-service__atc">
      <button
        type="button"
        class="btn btn--primary klm-atc klm-service__atc-btn"
        id="klm-atc-2"
        onclick="document.getElementById('klm-form').submit()">
        <svg width="18" height="18" viewBox="0 0 18 18" fill="none" aria-hidden="true"><path d="M1 1h2.5l1.7 8.6a2 2 0 0 0 2 1.4h7.3a2 2 0 0 0 2-1.6L17.5 4H4.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/><circle cx="7" cy="15.5" r="1.5" stroke="currentColor" stroke-width="1.4"/><circle cx="13.5" cy="15.5" r="1.5" stroke="currentColor" stroke-width="1.4"/></svg>
        Add to Basket
      </button>
      <div class="klm-service__payment-icons">
        {%- render 'payment-icons', extended: true -%}
      </div>
    </div>
  </div>
</section>

{%- comment -%} ── FAQ ─────────────────────────────────────────────────────── {%- endcomment -%}
<section class="klm-faq" id="klm-faq">
  <div class="container">
    <h2 class="klm-section__heading">Frequently Asked Questions</h2>
    <div class="klm-faq__list">
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What type of mattress is the Klawford Duo Sleep?</summary>
        <div class="klm-faq__a">The Klawford Duo Sleep is a 31 cm premium foam mattress featuring a 25 cm orthopaedic foam core, three layers of medium-firm comfort foam, and a premium bamboo knitted cover. It is designed to deliver balanced comfort and elevated luxury — supportive enough for back sleepers, plush enough for side and combination sleepers.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What is the orthopaedic foam core?</summary>
        <div class="klm-faq__a">The 25 cm orthopaedic foam core forms the foundation of the Duo Sleep. It is a high-density foam engineered to keep your spine in proper alignment while you sleep, distributing your body weight evenly and reducing pressure on your hips and lower back. The extra reinforcement throughout the core helps the mattress maintain its shape and support long term.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What are the triple comfort foam layers?</summary>
        <div class="klm-faq__a">Above the orthopaedic core, the Duo Sleep has three layers of medium-firm foam that work together to cushion pressure points at the shoulders, hips, and lower back. The layers are calibrated to provide a plush, comfortable feel on the surface while maintaining the firm base support underneath — so you get the best of both worlds.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What is the bamboo knitted cover and what does it do?</summary>
        <div class="klm-faq__a">The Duo Sleep is wrapped in a premium bamboo knitted fabric cover. Bamboo fibres are naturally breathable and moisture-wicking, which means the cover actively draws warmth and moisture away from your body during sleep. This helps regulate your sleeping temperature — keeping you cool in warmer months and comfortable throughout the year.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What firmness level is the Duo Sleep?</summary>
        <div class="klm-faq__a">The Duo Sleep is a medium-firm mattress — it provides solid, spine-aligning support from the orthopaedic foam core while the comfort foam layers on top add a plush, cushioned feel. This balance makes it suitable for back sleepers, side sleepers, and combination sleepers who want support without a hard feel.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">Is the Duo Sleep suitable for couples?</summary>
        <div class="klm-faq__a">Yes — the Duo Sleep is specifically designed with couples in mind. The foam construction absorbs and minimises partner movement, which means disturbances on one side of the mattress are less likely to be felt on the other. It is listed as ideal for couples and combination sleepers on the product spec sheet.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">Do your products conform to UK fire safety regulations?</summary>
        <div class="klm-faq__a">Yes — all Klawford mattresses meet UK fire safety regulations under The Furniture and Furnishings (Fire Safety) Regulations 1988 (amended). You can sleep safely and confidently in every mattress we sell.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">What sizes is the Duo Sleep available in?</summary>
        <div class="klm-faq__a">The Klawford Duo Sleep is available in all standard UK sizes — Small Single (2ft 6), Single (3ft), Small Double (4ft), Double (4ft 6), King (5ft), and Super King (6ft). Select your preferred size using the size selector on the product page.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">Is delivery free on the Duo Sleep?</summary>
        <div class="klm-faq__a">Yes — free delivery is included with every Klawford Duo Sleep purchase. We deliver to the room of your choice with a 2-man team, so you do not need to manage heavy packaging alone. You can select your preferred delivery date at checkout. The mattress also comes with a 60-night risk-free trial — if it is not right for you, contact us and we will arrange a return and full refund.</div>
      </details>
      <details class="klm-faq__item">
        <summary class="klm-faq__q">Can I spread the cost of the Duo Sleep?</summary>
        <div class="klm-faq__a">Yes — we offer 0% interest-free financing through DivideBuy, letting you spread the cost over up to 12 months at no extra charge. Klarna is also available at checkout for pay-later or 3-payment options. Select your preferred payment method when you proceed to checkout.</div>
      </details>
    </div>
  </div>
</section>

<script>
(function(){
  var variants = {{ product.variants | json }};

  function updateSizePriceDiffs(selectedPrice) {
    document.querySelectorAll('.klm-size-btn').forEach(function(b) {
      var btnPrice = parseInt(b.dataset.price, 10);
      var priceEl  = b.querySelector('[data-role="price"]');
      if (!priceEl) return;
      if (b.classList.contains('is-active')) {
        priceEl.textContent = formatMoney(btnPrice);
        priceEl.className = 'klm-size-btn__price';
      } else {
        var diff = btnPrice - selectedPrice;
        if (diff < 0) {
          priceEl.textContent = '-' + formatMoney(Math.abs(diff));
          priceEl.className = 'klm-size-btn__price klm-size-btn__price--save';
        } else if (diff > 0) {
          priceEl.textContent = '+' + formatMoney(diff);
          priceEl.className = 'klm-size-btn__price';
        } else {
          priceEl.textContent = formatMoney(btnPrice);
          priceEl.className = 'klm-size-btn__price';
        }
      }
    });
  }

  document.querySelectorAll('.klm-size-btn').forEach(function(btn){
    btn.addEventListener('click', function(){
      document.querySelectorAll('.klm-size-btn').forEach(function(b){
        b.classList.remove('is-active');
        b.setAttribute('aria-pressed', 'false');
      });
      btn.classList.add('is-active');
      btn.setAttribute('aria-pressed', 'true');

      var vid     = btn.dataset.vid;
      var price   = parseInt(btn.dataset.price, 10);
      var compare = parseInt(btn.dataset.compare, 10);
      var name    = btn.dataset.size;

      var nameEl = document.getElementById('klm-size-name');
      if (nameEl) nameEl.textContent = name;

      var dimsEl = document.getElementById('klm-size-dims');
      if (dimsEl) {
        var dw = btn.dataset.width;
        var dl = btn.dataset.length;
        if (dw && dl) {
          dimsEl.innerHTML = '<span>Width: ' + dw + 'cm</span><span>Length: ' + dl + 'cm</span>';
          dimsEl.style.display = '';
        } else {
          dimsEl.style.display = 'none';
        }
      }

      var priceEl   = document.getElementById('klm-price');
      var compareEl = document.getElementById('klm-compare');
      var pctEl     = document.getElementById('klm-pct');
      if (priceEl) priceEl.textContent = formatMoney(price);
      if (compareEl && pctEl) {
        if (compare > price) {
          compareEl.textContent = formatMoney(compare);
          compareEl.style.display = '';
          var pct = Math.round((compare - price) / compare * 100);
          pctEl.textContent = pct + '% OFF';
          pctEl.style.display = '';
        } else {
          compareEl.style.display = 'none';
          pctEl.style.display = 'none';
        }
      }

      updateSizePriceDiffs(price);

      var inp = document.getElementById('klm-variant-id');
      if (inp) inp.value = vid;

      var atcBtns = document.querySelectorAll('.klm-atc');
      var variant = variants.find(function(v){ return String(v.id) === String(vid); });
      var avail = variant ? variant.available : true;
      atcBtns.forEach(function(b){ b.disabled = !avail; });
      var labelEl = document.getElementById('klm-atc-label');
      if (labelEl) labelEl.textContent = avail ? 'Add to Basket' : 'Sold Out';
    });
  });

  function formatMoney(cents){
    return '£' + (cents / 100).toFixed(2).replace(/\.00$/, '');
  }

  var thumbsEl = document.getElementById('klm-thumbs');
  var mainImg  = document.getElementById('klm-main-img');

  if (thumbsEl && mainImg) {
    thumbsEl.addEventListener('click', function(e){
      var thumb = e.target.closest('.klm-gallery__thumb');
      if (!thumb) return;
      mainImg.src = thumb.dataset.src;
      mainImg.removeAttribute('srcset');
      mainImg.removeAttribute('sizes');
      var iw = parseInt(thumb.dataset.width, 10);
      var ih = parseInt(thumb.dataset.height, 10);
      mainImg.style.objectFit = (ih > iw) ? 'contain' : 'cover';
      document.querySelectorAll('.klm-gallery__thumb').forEach(function(t){ t.classList.remove('is-active'); });
      thumb.classList.add('is-active');
    });
  }

  var prevBtn = document.getElementById('klm-thumb-prev');
  var nextBtn = document.getElementById('klm-thumb-next');
  if (prevBtn && thumbsEl) prevBtn.addEventListener('click', function(){ thumbsEl.scrollBy({ left: -200, behavior: 'smooth' }); });
  if (nextBtn && thumbsEl) nextBtn.addEventListener('click', function(){ thumbsEl.scrollBy({ left: 200, behavior: 'smooth' }); });

  var featData = [
    { name: 'Orthopaedic Foam Core', body: 'The Duo Sleep is built around a 25 cm orthopaedic foam core engineered for spine alignment and firm, even support across the entire sleeping surface — giving your back the reinforcement it needs, night after night.' },
    { name: 'Bamboo Knitted Cover', body: 'The Duo Sleep is wrapped in a premium bamboo knitted fabric that is naturally breathable and moisture-wicking. The cover actively regulates your sleeping temperature, drawing warmth and moisture away from the surface to keep you cool and comfortable.' },
    { name: 'Triple Comfort Foam Layers', body: 'Above the orthopaedic core, three layers of medium-firm foam work together to cushion pressure points at the shoulders, hips, and lower back — providing a plush, comfortable feel on the surface while the core delivers firm underlying support.' },
    { name: 'Pressure Relief', body: 'The layered foam construction of the Duo Sleep contours gently to your body shape, cushioning pressure points and distributing weight evenly across the sleeping surface — reducing the risk of waking up with stiffness or soreness.' },
    { name: 'Temperature Regulation', body: 'The bamboo knitted cover naturally regulates your sleeping temperature by wicking moisture and drawing warmth away from the body. This keeps you at the ideal sleep temperature throughout the night — comfortable in every season.' },
    { name: 'Couple-Friendly', body: 'The foam construction of the Duo Sleep absorbs and minimises partner movement, which means that shifting or getting up on one side of the mattress is less likely to disturb the other sleeper — making it an ideal choice for couples.' },
    { name: 'Medium-Firm Feel', body: 'The Duo Sleep delivers a medium-firm feel that is balanced and versatile — firm enough to keep your spine aligned, with enough surface give to cushion your pressure points. Suitable for back sleepers, side sleepers, and combination sleepers.' },
    { name: 'UK Quality', body: 'Every Klawford mattress is built to a high standard using quality materials — engineered for consistent comfort, durability, and long-lasting performance night after night.' }
  ];

  var titleEl = document.getElementById('klm-feat-title');
  var bodyEl  = document.getElementById('klm-feat-body');
  var pipEl   = document.getElementById('klm-feat-pip');

  function movePip(activeBtn) {
    if (!pipEl) return;
    var track = document.querySelector('.klm-features__track');
    if (!track) return;
    var trackRect = track.getBoundingClientRect();
    var btnRect   = activeBtn.getBoundingClientRect();
    var center    = btnRect.left + btnRect.width / 2 - trackRect.left;
    pipEl.style.left = center + 'px';
  }

  document.querySelectorAll('.klm-feat').forEach(function(btn, i){
    btn.addEventListener('click', function(){
      document.querySelectorAll('.klm-feat').forEach(function(b){
        b.classList.remove('is-active');
        b.setAttribute('aria-selected', 'false');
      });
      btn.classList.add('is-active');
      btn.setAttribute('aria-selected', 'true');
      if (titleEl && featData[i]) titleEl.textContent = featData[i].name;
      if (bodyEl  && featData[i]) bodyEl.textContent  = featData[i].body;
      movePip(btn);
    });
  });

  var firstFeat = document.querySelector('.klm-feat.is-active');
  if (firstFeat) {
    setTimeout(function(){ movePip(firstFeat); }, 50);
  }
  window.addEventListener('resize', function(){
    var activeFeat = document.querySelector('.klm-feat.is-active');
    if (activeFeat) movePip(activeFeat);
  });

  document.querySelectorAll('.klm-faq__item').forEach(function(det){
    det.addEventListener('toggle', function(){
      if (det.open) {
        document.querySelectorAll('.klm-faq__item').forEach(function(d){
          if (d !== det) d.open = false;
        });
      }
    });
  });

})();
</script>

{% schema %}
{
  "name": "Duo Sleep Product",
  "settings": [

    { "type": "text", "id": "dividebuy_text", "label": "DivideBuy info line", "default": "Spread the cost interest-free over 12 months, 0% credit." },
    { "type": "text", "id": "klarna_text", "label": "Klarna info line", "default": "Pay later in 30 days or split into 3 interest-free payments." }
  ]
}
{% endschema %}
