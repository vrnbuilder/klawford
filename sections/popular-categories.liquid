<section class="popular-categories section">
  <div class="container">
    <h2 class="section-title">{{ section.settings.heading }}</h2>
    <div class="categories-grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'category' -%}

          {%- assign category_label = block.settings.label -%}
          {%- assign category_url = block.settings.url | default: '/collections/all' -%}
          {%- if category_label == 'Sofas' -%}
            {%- assign category_label = 'Rugby Beds' -%}
            {%- assign category_url = '/collections/rugby-bed-sets' -%}
          {%- endif -%}

          {%- comment -%} Pick a fallback image based on the category label {%- endcomment -%}
          {%- case category_label -%}
            {%- when 'Beds' -%}
              {%- assign fallback_img = 'klawford-category-beds.jpg' | asset_url -%}
            {%- when 'Mattresses' -%}
              {%- assign fallback_img = 'klawford-category-mattresses.jpg' | asset_url -%}
            {%- when 'Rugby Beds' -%}
              {%- assign fallback_img = 'klawford-category-rugby-beds.jpg' | asset_url -%}
            {%- when 'Headboards' -%}
              {%- assign fallback_img = 'klawford-category-headboards.jpg' | asset_url -%}
            {%- else -%}
              {%- assign fallback_img = 'https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=800&q=85' -%}
          {%- endcase -%}

          <a href="{{ category_url }}" class="category-card" {{ block.shopify_attributes }}>
            <div class="category-card__img">

              {%- if block.settings.image != blank -%}
                <img
                  src="{{ block.settings.image | image_url: width: 600 }}"
                  srcset="{{ block.settings.image | image_url: width: 400 }} 400w,
                          {{ block.settings.image | image_url: width: 600 }} 600w"
                  sizes="(max-width: 768px) 50vw, 25vw"
                  alt="{{ category_label | escape }}"
                  loading="lazy"
                  width="400"
                  height="533">
              {%- else -%}
                <div class="category-card__bg-img" style="background-image: url('{{ fallback_img }}');"></div>
              {%- endif -%}

              <div class="category-card__overlay" aria-hidden="true"></div>
              <div class="category-card__label">
                <span class="category-card__name">{{ category_label }}</span>
                <span class="category-card__cta-text">
                  Shop Now
                  <svg width="12" height="12" viewBox="0 0 12 12" fill="none"><path d="M2.5 6h7M6.5 2.5L10 6l-3.5 3.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </span>
              </div>

            </div>
          </a>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Popular Categories",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading",
      "default": "Popular Categories"
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Category image (upload your own)"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Category name",
          "info": "Use exactly: Beds, Mattresses, Rugby Beds, or Headboards to auto-load matching image",
          "default": "Beds"
        },
        {
          "type": "url",
          "id": "url",
          "label": "Category link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Popular Categories",
      "blocks": [
        { "type": "category", "settings": { "label": "Beds",        "url": "/collections/beds" } },
        { "type": "category", "settings": { "label": "Mattresses",  "url": "/collections/mattresses" } },
        { "type": "category", "settings": { "label": "Rugby Beds",  "url": "/collections/rugby-bed-sets" } },
        { "type": "category", "settings": { "label": "Headboards",  "url": "/collections/headboards" } }
      ]
    }
  ]
}
{% endschema %}
